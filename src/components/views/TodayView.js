import { useState } from 'react';

const TodayView = ({ selectedDate, tasks, stats, onAddTask, onToggleTask, onDeleteTask }) => {
  // Solo mostrar el Hero Section, sin cards adicionales
  return null;
};

export default TodayView;